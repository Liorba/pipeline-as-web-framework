"""Future integrations — intentionally not wired in the happy-path demo.

TBD: Iceberg
------------
- Add an ``IcebergLoader`` collaborator injected into ``load_summary``
- Configure REST/JDBC catalog via Settings (no credentials required for local REST fixture)

TBD: OpenLineage
----------------
- Register an Airflow listener or use ``openlineage-airflow`` to emit dataset/job facets
- Map extract/transform/load datasets to fixture paths and summary output
"""

# Placeholder module — see README "Future work" section.
